# Retired frontend

This directory is not an operational MoonCen frontend. It contains the former
Google Maps implementation and its normal `dev`, `build`, and `preview`
commands intentionally fail closed.

Use `../frontend2`, which is the deployed Kakao Maps frontend.
