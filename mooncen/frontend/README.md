# Archived web prototype

This React 19 application is retained only as a design/reference prototype.
Production uses `frontend2/`; this directory is excluded from release archives.

Do not add production routes or deployment configuration here. Security updates and
lint/build checks remain enabled so the reference code does not become an unsafe
copy-and-paste source.
